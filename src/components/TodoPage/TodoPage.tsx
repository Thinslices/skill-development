import type { NextPage } from "next"
import { Layout } from "../";

export const TodoPage:NextPage = () => {

    return (
        <Layout>
            <div className="h1">Todo</div>
        </Layout>
    )
}